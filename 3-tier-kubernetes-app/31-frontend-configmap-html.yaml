apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-html
  namespace: three-tier
data:
  index.html: |
    <!doctype html>
    <html>
      <head>
        <meta charset="utf-8" />
        <title>3-Tier App on Kubernetes</title>
      </head>
      <body>
        <h1>Frontend: NGINX static site</h1>
        <p>Backend API is proxied at <code>/api</code>.</p>
        <ul>
          <li><a href="/api/">Test API via /api/</a></li>
        </ul>
      </body>
    </html>
